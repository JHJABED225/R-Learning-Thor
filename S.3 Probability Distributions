


# Probability Distribution 
    # Probability distribution describes the probability of each possible outcomes in a scenario. 
    # Expected value = mean of a distribution = sum(each outcome * their probability) 
    # Probability = Selected area of probability distribution.
    # Discrete uniform distribution = Each outcome has same probability (?) 
    # Probability = Area under distribution. 


# Binomial distribution: 
    
    #rbinom(no. of trials, no. of coins, probability of head/success) 

# Uniform Continuous Distribution: 

    # P(4<= wait_time <=7) = p(wait_time <=7) - p(wait_time<=4) 
            = punif(7, min= 0, max= 12) - punif(4, min= 0, max= 12) 



# Law of large number: 
    # As the sample size increases, the sample mean will approach the expected mean, i.e theoretical mean. 
    

#===========================================================================================================================
#
#===========================================================================================================================

# Probability of closing > 1 deal out of 3 deals
1- pbinom(1,3, 0.3, lower.tail= FALSE)



#===========================================================================================================================
# Task: Increase mean and sd 20 and 30 percent of previous (mean= 5000, sd= 2000) and generate 36
#===========================================================================================================================
# Calculate new average amount
new_mean <- 5000 + .2 * 5000

# Calculate new standard deviation
new_sd <- 2000 + .3 * 2000

# Simulate 36 sales
new_sales <- new_sales %>% 
  mutate(amount = rnorm(36, new_mean, new_sd))

# Create histogram with 10 bins
 ggplot(new_sales, aes(amount)) + geom_histogram(bins= 10)
